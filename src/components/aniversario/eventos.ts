/** Constantes compartidas entre el overlay y el mono (sin lógica pesada). */
export const SESSION_KEY_OVERLAY = "mm_overlay_aniversario_visto";
/** Evento que avisa al mono (MonkeyWalker) de que el overlay se cerró. */
export const EVENTO_OVERLAY_CERRADO = "mm:overlay-cerrado";
